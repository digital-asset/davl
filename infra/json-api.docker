FROM openjdk:8-alpine

RUN mkdir /app
COPY json-api.jar /app/json-api.jar
COPY logback.xml /app/logback.xml

WORKDIR /app
ENTRYPOINT ["java", "-Dlogback.configurationFile=/app/logback.xml", "-jar", "/app/json-api.jar", "--application-id", "DAVL-JSON"]
